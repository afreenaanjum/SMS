const font = "Lato, sans-serif";
const styles = (theme) => ({
  notchedOutlineSelect: {
    borderColor: "#E45FFF !important",
    width: "100% important",
  },
  formCont: {
    width: "10% !important",
  },
});

export default styles;
